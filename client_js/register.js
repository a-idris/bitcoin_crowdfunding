const wallet = require('/src/wallet_management.js')

$(document).ready(function() {
    $('form#register').on('submit', function(event) {
        // popup
        // generate pass phrase. make visible with suggested pass phrase. 
    });
});


